{
  "name": "codap-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "api:dev": "pnpm --filter codap-api dev",
    "api:start": "pnpm --filter codap-api start",
    "api:build": "pnpm --filter codap-api build:ts",
    "api:test": "pnpm --filter codap-api test",
    "api:add": "pnpm --filter codap-api add",
    "api:seed": "pnpm --filter codap-api seed",
    "app:start": "pnpm --filter codap start",
    "app:android": "pnpm --filter codap android",
    "app:ios": "pnpm --filter codap ios",
    "app:web": "pnpm --filter codap web",
    "app:add": "pnpm --filter codap add"
  }
}
