# URL da API do Codap. O dashboard é uma ferramenta de desenvolvimento e as
# rotas de escrita do catálogo só existem quando a API roda com
# NODE_ENV=development (`pnpm api:dev`).
VITE_API_URL=http://localhost:3000
